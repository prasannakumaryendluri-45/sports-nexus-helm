apiVersion: v1
kind: Service
metadata:
  name: sports-nexus-service
spec:
  type: {{ .Values.service.type }}
  selector:
    app: sports-nexus
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
